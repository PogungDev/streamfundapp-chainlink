"use client"

// EngineNotifications component - kept for app structure but notifications disabled
export function EngineNotifications() {
  // Component kept for structure, but notifications are disabled
  // to prevent annoying popups while maintaining app functionality
  
  return null
}
